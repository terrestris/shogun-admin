import InstancePermissionGrid from './InstancePermissionGrid';

describe('<InstancePermissionGrid />', () => {

  it('is defined', () => {
    expect(InstancePermissionGrid).not.toBeUndefined();
  });

});
