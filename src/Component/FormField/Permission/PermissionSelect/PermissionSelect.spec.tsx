import { describe, it, expect } from 'vitest';

import PermissionSelect from './PermissionSelect';

describe('<PermissionSelect />', () => {

  it('is defined', () => {
    expect(PermissionSelect).not.toBeUndefined();
  });

});
