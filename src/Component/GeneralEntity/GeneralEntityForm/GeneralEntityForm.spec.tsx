import { describe, it, expect } from 'vitest';

import { GeneralEntityForm } from './GeneralEntityForm';

describe('<GeneralEntityForm />', () => {

  it('is defined', () => {
    expect(GeneralEntityForm).not.toBeUndefined();
  });

});
