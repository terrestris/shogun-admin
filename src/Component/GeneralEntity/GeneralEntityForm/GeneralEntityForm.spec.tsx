import GeneralEntityForm from './GeneralEntityForm';


describe('<GeneralEntityForm />', () => {
  it('is defined', () => {
    expect(GeneralEntityForm).not.toBeUndefined();
  });
  //TODO: Test if GeneralEntityForm can be rendered (current issue: monaco-editor not working with jest and react testing library)
});

